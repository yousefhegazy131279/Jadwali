# Jadwali 1.0 deployment record

Database migrations 202609240001–202609240003 were applied to aoaacxylmuzkdgwaufmc on 2026-09-25 through the Supabase SQL editor in a single transaction. Verification returned zero auth users missing a profile. Existing RLS policies were backed up to the private jadwali_backup.policies_v1 table before replacement. Do not re-run these migrations on that database. No user data was removed.

The generate-schedule Edge Function is deployed with JWT verification enabled and an additional auth.getUser-equivalent check inside the function. Browser deployment bundles the shared validation module into index.ts. OPENAI_API_KEY is intentionally not configured: the owner requested deferring AI activation. To activate, add OPENAI_API_KEY in Supabase → Edge Functions → Secrets, with an API project that has billing/credits, then test an authenticated request from the production site. No API secret belongs in NEXT_PUBLIC variables. Default allowed origin is https://jadwaly-hgz.vercel.app; set ALLOWED_ORIGINS to a comma-separated list if adding domains. Quota: 30 requests/user/day.

Validation: TypeScript; six Node tests including a PostgreSQL-compatible PGlite run of all migrations, duplicate completion, session ordering, profile trigger, viewer/editor/outsider RLS, removed-member revocation, and atomic AI quota; Next.js production build. Browser QA uses tests/fixture-server.cjs with process-only local environment overrides, never production accounts or data. Covered onboarding, pause/reload/resume and completion, language/theme, prayer saving and planner loading, sharing UI, projects and analytics, and AI draft application with a canned response. SQL permissions are tested by PGlite, not by the mock server. Historical focus dates are not fabricated. Live AI response testing is deferred.

Local setup: npm ci, copy .env.example to .env.local and configure public Supabase values, npm run typecheck, npm test, npm run build. For a NEW database, the three migrations assume the original Jadwali schema already exists; they are upgrade migrations. For later function changes use supabase functions deploy generate-schedule --project-ref aoaacxylmuzkdgwaufmc with an authorized CLI session.
